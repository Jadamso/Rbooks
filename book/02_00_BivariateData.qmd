# Bivariate Data

This part extends the same data-first approach to relationships between two variables.
It covers bivariate distributions, group comparisons, association statistics, linear and local regression, prediction-focused inference, and the distinction between those tools and causal analysis.
The emphasis is how to choose, evaluate, and communicate bivariate models using real data in R without making causal claims that the design cannot support.
